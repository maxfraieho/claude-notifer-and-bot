# 🎯 РОЗШИРЕНИЙ АУДИТ ДОСВІДУ КОРИСТУВАЧА

**Згенеровано:** 2025-09-15 04:49:45

**Проаналізовано файлів:** 57


## 📊 ЗАГАЛЬНИЙ ЗВІТ

- **Всього проблем знайдено:** 807

- **🔴 Критичних (потрібно виправити негайно):** 406

- **🟠 Високого пріоритету (потрібно виправити цього тижня):** 79

- **🟡 Середнього пріоритету (поліпшення інтерфейсу):** 281


## 🔴 КРИТИЧНІ ПРОБЛЕМИ (ВИПРАВИТИ НЕГАЙНО)

### C1: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/help` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/help` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C2: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/pwd` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/pwd` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C3: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/actions` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/actions` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C4: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/history` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/history` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C5: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/explain` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/explain` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C6: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/ls` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/ls` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C7: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/start` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/start` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C8: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/add_schedule` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/add_schedule` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C9: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/new` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/new` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C10: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/schedules` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/schedules` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C11: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/debug` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/debug` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C12: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/settings` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/settings` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C13: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/git` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/git` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C14: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/export` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/export` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C15: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/cd` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/cd` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C16: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/projects` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/projects` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C17: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/continue` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/continue` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C18: НЕПРАЦЮЮЧА КОМАНДА

**Проблема:** Команда `/status` оголошена, але не реалізована

**Що бачить користувач:** Набирає `/status` → отримує помилку або нічого

**Рішення:** Реалізувати обробник команди або прибрати з довідки/меню


### C19: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
    )

    parser.add_argument(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C20: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    return parser.parse_args()


async def create_application(config: Settings) -> Dict[str, Any]:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C21: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        sys.exit(0)


if __name__ == `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C22: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) if chat_id.strip()]
        if isinstance(v, int):
            return [v]
        if isinstance(v, list):
            return v
        return []


class Settings(BaseSettings):
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C23: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) if uid.strip()]
    #     if isinstance(v, int):
    #         return [v]  # Convert single int to list
    #     if isinstance(v, list):
    #         return v  # Already a list
    #     # If we can`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C24: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `])

        # Parse JSON fields
        if data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C25: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `])

        # Parse JSON fields
        if data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C26: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {}),
                    timestamp=datetime.utcnow(),
                    success=not response.is_error,
                    error_message=response.error_type if response.is_error else None,
                )
                await self.tools.save_tool_usage(tool_usage)

        # Update cost tracking
        await self.costs.update_daily_cost(user_id, response.cost)

        # Update user stats
        user = await self.users.get_user(user_id)
        if user:
            user.total_cost += response.cost
            user.message_count += 1
            user.last_active = datetime.utcnow()
            await self.users.update_user(user)

        # Update session stats
        session = await self.sessions.get_session(session_id)
        if session:
            session.total_cost += response.cost
            session.total_turns += response.num_turns
            session.message_count += 1
            session.last_used = datetime.utcnow()
            await self.sessions.update_session(session)

        # Log audit event
        audit_event = AuditLogModel(
            id=None,
            user_id=user_id,
            event_type=`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C27: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
            security=self.deps.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C28: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )

        # Try to notify user
        if update and update.effective_message:
            try:
                await update.effective_message.reply_text(user_message)
            except Exception:
                logger.exception(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C29: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        if audit_logger and update and update.effective_user:
            try:
                await audit_logger.log_security_violation(
                    user_id=update.effective_user.id,
                    violation_type=`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C30: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger = data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C31: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return  # Block processing

    # Validate file uploads if present
    if message and message.document:
        is_safe, error_message = await validate_file_upload(
            message.document, security_validator, user_id, audit_logger
        )
        if not is_safe:
            await message.reply_text(
                f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C32: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    # Track user behavior patterns
    user_behavior = data.setdefault(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C33: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `: None,
        },
    )

    import time

    current_time = time.time()

    if user_data[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C34: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `This activity has been logged.\n\n`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C35: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger = data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C36: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
        return

    # Check if user is already authenticated
    if auth_manager.is_authenticated(user_id):
        # Update session activity
        if auth_manager.refresh_session(user_id):
            session = auth_manager.get_session(user_id)
            logger.debug(
                `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C37: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        )

    if authentication_successful:
        session = auth_manager.get_session(user_id)
        logger.info(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C38: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `Please contact the administrator for access.\n\n`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C39: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not auth_manager or not auth_manager.is_authenticated(user_id):
        if event.effective_message:
            await event.effective_message.reply_text(
                `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C40: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not auth_manager or not auth_manager.is_authenticated(user_id):
        if event.effective_message:
            await event.effective_message.reply_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C41: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
        return

    # Check for admin permissions (placeholder logic)
    permissions = session.user_info.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C42: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger = data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C43: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        return  # Stop processing

    # Rate limit check passed
    logger.debug(
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C44: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    # Store start time for duration tracking
    import time

    start_time = time.time()

    try:
        # Execute the handler
        result = await handler(event, data)

        # Calculate processing time
        processing_time = time.time() - start_time

        # Get actual cost from context if available
        actual_cost = data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C45: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
    user_burst_data = burst_tracker.setdefault(
        user_id, {`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C46: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `re sending requests too quickly. `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C47: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
                )
        else:
            if event.effective_message:
                await event.effective_message.reply_text(
                    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C48: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) for tool in response.tools_used]

        # Update active files if file tools were used
        if any(tool in self.last_tools_used for tool in [`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C49: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) for tool in response.tools_used]

        if `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C50: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
            lines.append(f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C51: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `).isoformat()
                    if session.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C52: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, [])
            for msg in recent_messages:
                content = msg.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C53: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `quick_actions.{action.id}.name`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C54: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        # Detect language
        language = self._detect_language(file_path.suffix)

        # Create prompt
        prompt = f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C55: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `).lower()
                for framework in possible_frameworks:
                    if framework.lower() in content:
                        frameworks.append(framework)

        # Check for specific framework files
        if (directory / `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C56: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                    # Count TODOs and FIXMEs
                    todo_count += content.upper().count(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C57: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
        
        # Check Claude availability
        if conditions.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C58: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, False):
            if not self._is_dnd_time():
                return False, `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C59: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 0)
        if no_activity_hours > 0:
            if await self._check_user_activity(no_activity_hours):
                return False, f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C60: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
        if not schedule:
            return False
        
        timezone = ZoneInfo(schedule.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C61: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        target_time_str = schedule.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C62: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `).lower()
            day_map = {
                `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C63: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        logger.info(f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C64: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            success, output = await self._execute_claude_prompt(prompt_text)
            
            if success:
                logger.info(f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C65: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {}).get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C66: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {}).get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C67: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, [])
        if not prompts:
            return
        
        # Check if any prompts need execution
        prompts_to_execute = []
        for prompt in prompts:
            if self._is_time_to_execute(prompt):
                prompts_to_execute.append(prompt)
        
        if not prompts_to_execute:
            return
        
        logger.info(f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C68: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `))
                    last_execution = dt.strftime(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C69: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            if localization:
                result = localization.get(key, language=`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C70: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `: None}))
        if not self.transitions_log.exists():
            self.transitions_log.touch()

    def parse_limit_message(self, output: str) -> Optional[datetime]:
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C71: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 
                                         timestamp=now.strftime(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C72: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 
                                         timestamp=now.strftime(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C73: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 
                                                time=reset_local.strftime(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C74: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, False)
            last_reason = last_state_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C75: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            last_reset_expected = datetime.fromisoformat(last_reset_expected_str) if last_reset_expected_str else None
            last_check_str = last_state_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C76: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
            if line.startswith(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C77: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)) == 4:
                # Commit info line
                parts = line.split(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C78: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C79: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, name=update.effective_user.first_name)
        description_text = await t(context, user_id, `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C80: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if audit_logger:
        audit_logger_typed = cast(AuditLogger, audit_logger)
        await audit_logger_typed.log_command(
            user_id=user_id, command=`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C81: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C82: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {}).get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C83: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C84: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings_typed.approved_directory
    ) if context.user_data else settings_typed.approved_directory
    relative_path = current_dir.relative_to(settings_typed.approved_directory)

    # Clear any existing session data
    if context.user_data:
        context.user_data[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C85: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C86: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C87: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C88: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings_typed.approved_directory
    ) if context.user_data else settings_typed.approved_directory

    status_msg = None
    try:
        if not claude_integration:
            # Get localized error message
            localization = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C89: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) if context.user_data else None

        if claude_session_id:
            # We have a session in context, continue it directly
            # Get localized continuation messages
            localization = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C90: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, relative_path=str(current_dir.relative_to(settings_typed.approved_directory)))
                
                if prompt:
                    process_msg = await t(context, user_id, `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C91: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            user_language_storage = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C92: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `Directory: `{current_dir.relative_to(settings_typed.approved_directory)}/`\n\n`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C93: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
        )

        # Log failed continue
        if audit_logger:
            audit_logger_typed = cast(AuditLogger, audit_logger)
            await audit_logger_typed.log_command(
                user_id=user_id,
                command=`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C94: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C95: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    # Get current directory
    current_dir = context.user_data.get(
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C96: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C97: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C98: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `• `/cd ..` - Go up one level\n`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C99: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings_typed.approved_directory
    ) if context.user_data else settings_typed.approved_directory

    try:
        # Validate path using security validator
        if security_validator:
            security_validator_typed = cast(SecurityValidator, security_validator)
            valid, resolved_path, error = security_validator_typed.validate_path(
                target_path, current_dir
            )

            if not valid:
                await message.reply_text(f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C100: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return

        if not resolved_path.is_dir():
            await message.reply_text(
                f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C101: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
        )

        # Log successful command
        if audit_logger:
            audit_logger_typed = cast(AuditLogger, audit_logger)
            await audit_logger_typed.log_command(user_id, `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C102: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C103: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings_typed.approved_directory
    ) if context.user_data else settings_typed.approved_directory

    relative_path = current_dir.relative_to(settings_typed.approved_directory)
    absolute_path = str(current_dir)

    # Add quick navigation buttons
    keyboard = [
        [
            InlineKeyboardButton(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C104: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        parse_mode=None,
        reply_markup=reply_markup,
    )


async def show_projects(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C105: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C106: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return

        # Create inline keyboard with project buttons
        keyboard = []
        for i in range(0, len(projects), 2):
            row = []
            for j in range(2):
                if i + j < len(projects):
                    project = projects[i + j]
                    row.append(
                        InlineKeyboardButton(
                            f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C107: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
            reply_markup=reply_markup,
        )

    except Exception as e:
        await message.reply_text(f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C108: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C109: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) if context.user_data else None
    current_dir = context.user_data.get(
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C110: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            current_cost = cost_usage.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C111: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings_typed.claude_max_cost_per_user)
            cost_percentage = (current_cost / cost_limit) * 100 if cost_limit > 0 else 0

            usage_info = f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C112: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `.join(status_lines), parse_mode=None, reply_markup=reply_markup
    )


async def export_session(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C113: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    # Check if session export is available
    session_exporter = features.get_session_export() if features else None

    if not session_exporter:
        await message.reply_text(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C114: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) if context.user_data else None

    if not claude_session_id:
        await message.reply_text(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C115: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        parse_mode=None,
        reply_markup=reply_markup,
    )


async def end_session(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C116: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C117: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) if context.user_data else None

    if not claude_session_id:
        await message.reply_text(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C118: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings_typed.approved_directory
    ) if context.user_data else settings_typed.approved_directory
    relative_path = current_dir.relative_to(settings_typed.approved_directory)

    # Clear session data
    if context.user_data:
        context.user_data[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C119: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        parse_mode=None,
        reply_markup=reply_markup,
    )

    logger.info(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C120: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C121: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not features or not features.is_enabled(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C122: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )
        return

    # Get current directory
    current_dir = context.user_data.get(
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C123: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return

        # Get context-aware actions
        actions = await quick_action_manager.get_suggestions(
            session_data={`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C124: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return

        # Create inline keyboard with localization
        # user_id already defined above
        localization = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C125: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        user_lang = None
        
        if user_language_storage:
            try:
                user_lang = await user_language_storage.get_user_language(user_id)
            except:
                pass
        
        keyboard = quick_action_manager.create_inline_keyboard(
            actions, columns=2, localization=localization, user_lang=user_lang
        )

        # Get localized title for quick actions
        title_text = await t(context, user_id, `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C126: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        await message.reply_text(error_text, parse_mode=None)
        logger.error(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C127: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C128: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not features or not features.is_enabled(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C129: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )
        return

    # Get current directory
    current_dir = context.user_data.get(
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C130: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `Current directory `{current_dir.relative_to(settings_typed.approved_directory)}/` is not a git repository.\n\n`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C131: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        
        if not application or not settings:
            await message.reply_text(
                `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C132: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, [])
        system_settings = config.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C133: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `під час DND періоду (23:00-08:00).\n\n`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C134: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)} о {schedule.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C135: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C136: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )

    except Exception as e:
        logger.error(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C137: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `An error occurred while processing your request.\n`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C138: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )


async def handle_cd_callback(
    query, project_name: str, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C139: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger: AuditLogger = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C140: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
        )

        # Handle special paths
        if project_name == `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C141: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                return
            # Use the validated path
            new_path = resolved_path

        # Check if directory exists
        if not new_path.exists() or not new_path.is_dir():
            await query.edit_message_text(
                f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C142: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
            reply_markup=reply_markup,
        )

        # Log successful directory change
        if audit_logger:
            await audit_logger.log_command(
                user_id=user_id, command=`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C143: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )


async def handle_confirm_callback(
    query, confirmation_type: str, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C144: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    else:
        await query.edit_message_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C145: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return

        # Create project buttons
        keyboard = []
        for i in range(0, len(projects), 2):
            row = []
            for j in range(2):
                if i + j < len(projects):
                    project = projects[i + j]
                    row.append(
                        InlineKeyboardButton(
                            f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C146: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        
        keyboard.append(
            [
                InlineKeyboardButton(root_text, callback_data=`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C147: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
            reply_markup=reply_markup,
        )

    except Exception as e:
        await query.edit_message_text(f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C148: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
    )
    relative_path = current_dir.relative_to(settings.approved_directory)

    # Get localized button text
    user_id = query.from_user.id
    start_coding_text = await get_localized_text(context, user_id, `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C149: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        parse_mode=None,
        reply_markup=reply_markup,
    )


async def _handle_end_session_action(query, context: ContextTypes.DEFAULT_TYPE) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C150: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not claude_session_id:
        await query.edit_message_text(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C151: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
    )
    relative_path = current_dir.relative_to(settings.approved_directory)

    # Clear session data
    context.user_data[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C152: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        parse_mode=None,
        reply_markup=reply_markup,
    )


async def _handle_continue_action(query, context: ContextTypes.DEFAULT_TYPE) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C153: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    current_dir = context.user_data.get(
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C154: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        if claude_session_id:
            # Continue with the existing session (no prompt = use --continue)
            await query.edit_message_text(
                f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C155: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
                parse_mode=None,
            )

            claude_response = await claude_integration.continue_session(
                user_id=user_id,
                working_directory=current_dir,
                prompt=None,  # No prompt = use --continue
            )

        if claude_response:
            # Update session ID in context
            context.user_data[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C156: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** ` if len(claude_response.content) > 500 else `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C157: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `Directory: `{current_dir.relative_to(settings.approved_directory)}/`\n\n`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C158: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    current_dir = context.user_data.get(
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C159: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            current_cost = cost_usage.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C160: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.claude_max_cost_per_user)
            cost_percentage = (current_cost / cost_limit) * 100 if cost_limit > 0 else 0

            usage_info = f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C161: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `.join(status_lines), parse_mode=None, reply_markup=reply_markup
    )


async def _handle_ls_action(query, context: ContextTypes.DEFAULT_TYPE) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C162: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
    )

    try:
        # List directory contents (similar to /ls command)
        items = []
        directories = []
        files = []

        for item in sorted(current_dir.iterdir()):
            if item.name.startswith(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C163: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
    )


async def _handle_quick_actions_action(
    query, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C164: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        parse_mode=None,
        reply_markup=reply_markup,
    )


async def _handle_refresh_status_action(
    query, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C165: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
    )


async def handle_quick_action_callback(
    query, action_id: str, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C166: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C167: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        await query.edit_message_text(error_text, parse_mode=None)
        return

    # Get Claude integration
    claude_integration: ClaudeIntegration = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C168: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        await query.edit_message_text(error_text, parse_mode=None)
        return

    settings: Settings = context.bot_data[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C169: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
    )

    try:
        # Get the action from the manager
        action = quick_actions.actions.get(action_id)
        if not action:
            error_text = await get_localized_text(context, user_id, `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C170: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `quick_actions.{action.id}.name`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C171: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, action=action_display_name)
            await query.edit_message_text(error_text, parse_mode=None)
            return

        # Show execution message
        executing_text = await get_localized_text(context, user_id, `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C172: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, action=action_display_name)
            response_text = claude_response.content
            if len(response_text) > 4000:
                response_text = response_text[:4000] + `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C173: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, action=action_display_name)
            await query.edit_message_text(failed_text, parse_mode=None)

    except Exception as e:
        logger.error(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C174: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, action=action_id, error=str(e))
        await query.edit_message_text(error_text, parse_mode=None)


async def handle_followup_callback(
    query, suggestion_hash: str, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C175: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not conversation_enhancer:
        await query.edit_message_text(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C176: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )

        logger.info(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C177: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )


async def handle_conversation_callback(
    query, action_type: str, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C178: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `_Just type your request or upload files._`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C179: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        if conversation_enhancer:
            conversation_enhancer.clear_context(user_id)

        # Clear session data
        context.user_data[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C180: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
        )
        relative_path = current_dir.relative_to(settings.approved_directory)

        # Create quick action buttons
        keyboard = [
            [
                InlineKeyboardButton(
                    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C181: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
            reply_markup=reply_markup,
        )

        logger.info(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C182: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )


async def handle_git_callback(
    query, git_action: str, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C183: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not features or not features.is_enabled(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C184: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )
        return

    current_dir = context.user_data.get(
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C185: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )

    except Exception as e:
        logger.error(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C186: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )
        return

    session_exporter = features.get_session_export() if features else None
    if not session_exporter:
        await query.edit_message_text(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C187: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not claude_session_id:
        await query.edit_message_text(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C188: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `Generating {export_format.upper()} export...`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C189: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `Format: {exported_session.format.upper()}\n`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C190: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
        )

    except Exception as e:
        logger.error(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C191: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C192: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        keyboard.append([InlineKeyboardButton(back_text, callback_data=`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C193: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        
        await query.edit_message_text(select_message, reply_markup=reply_markup)
        
    elif param.startswith(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C194: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 1)[1]
        
        if localization.is_language_available(new_language):
            success = await user_language_storage.set_user_language(user_id, new_language)
            
            if success:
                # Get language name for confirmation
                lang_name = localization.get_available_languages().get(new_language, new_language.upper())
                
                # Get confirmation message in NEW language
                confirmation_text = localization.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C195: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                await query.edit_message_text(error_text)
        else:
            error_text = await get_user_text(localization, user_language_storage, user_id, `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C196: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        
        if not application or not settings:
            await query.edit_message_text(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C197: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, [])
            system_settings = config.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C198: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
                    reply_markup=reply_markup
                )
                return
            
            enabled_count = sum(1 for p in prompts if p.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C199: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)} о {schedule.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C200: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            
            enabled = system_settings.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C201: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            dnd_end = system_settings.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C202: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 0):.1f}с\n`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C203: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            
    except Exception as e:
        logger.error(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C204: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
            # Try to extract tool name from context if available
            tool_name = update_obj.metadata.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C205: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
                time_ms = update_obj.metadata[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C206: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            total_steps = update_obj.progress.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C207: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, []))
            model = update_obj.metadata.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C208: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger: Optional[AuditLogger] = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C209: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                return

        # Send typing indicator
        await update.message.chat.send_action(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C210: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            reply_to_message_id=update.message.message_id,
        )

        # Get Claude integration and storage from context
        claude_integration = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C211: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        if not claude_integration:
            await update.message.reply_text(
                `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C212: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
        )

        # Get existing session ID
        session_id = context.user_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C213: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            except Exception as e:
                logger.warning(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C214: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
                    reply_to_message_id=update.message.message_id if i == 0 else None,
                )

        # Update session info
        context.user_data[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C215: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        conversation_enhancer = (
            features.get_conversation_enhancer() if features else None
        )

        if conversation_enhancer and claude_response:
            try:
                # Update conversation context
                conversation_enhancer.update_context(user_id, claude_response)

                # Check if we should show follow-up suggestions
                if conversation_enhancer.should_show_suggestions(claude_response):
                    # Generate follow-up suggestions
                    suggestions = conversation_enhancer.generate_follow_up_suggestions(
                        claude_response.content,
                        claude_response.tools_used or [],
                        conversation_context,
                    )

                    if suggestions:
                        # Create keyboard with suggestions
                        suggestion_keyboard = (
                            conversation_enhancer.create_follow_up_keyboard(suggestions)
                        )

                        # Send follow-up suggestions
                        await update.message.reply_text(
                            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C216: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
    )
    audit_logger: Optional[AuditLogger] = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C217: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    logger.info(
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C218: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
                )

                # Log security violation
                if audit_logger:
                    await audit_logger.log_security_violation(
                        user_id=user_id,
                        violation_type=`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C219: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return

        # Check rate limit for file processing
        file_cost = _estimate_file_processing_cost(document.file_size)
        if rate_limiter:
            allowed, limit_message = await rate_limiter.check_rate_limit(
                user_id, file_cost
            )
            if not allowed:
                await update.message.reply_text(f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C220: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, parse_mode=None
        )

        # Check if enhanced file handler is available
        features = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C221: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
                    parse_mode=None,
                )

            except Exception as e:
                logger.warning(
                    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C222: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `• Source code files (.py, .js, .ts, etc.)\n`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C223: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, parse_mode=None
        )

        # Get Claude integration from context
        claude_integration = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C224: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
                parse_mode=None,
            )
            return

        # Get current directory and session
        current_dir = context.user_data.get(
            `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C225: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        # Process with Claude
        try:
            claude_response = await claude_integration.run_command(
                prompt=prompt,
                working_directory=current_dir,
                user_id=user_id,
                session_id=session_id,
            )

            # Update session ID
            context.user_data[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C226: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    image_handler = features.get_image_handler() if features else None

    if image_handler:
        try:
            # Send processing indicator
            progress_msg = await update.message.reply_text(
                `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C227: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, parse_mode=None
            )

            # Get Claude integration
            claude_integration = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C228: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
                    parse_mode=None,
                )
                return

            # Get current directory and session
            current_dir = context.user_data.get(
                `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C229: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

            # Process with Claude
            try:
                claude_response = await claude_integration.run_command(
                    prompt=processed_image.prompt,
                    working_directory=current_dir,
                    user_id=user_id,
                    session_id=session_id,
                )

                # Update session ID
                context.user_data[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C230: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `• Text files (.py, .js, .md, etc.)\n`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C231: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
    )

    for pattern in patterns:
        matches = re.findall(pattern, content, re.MULTILINE | re.IGNORECASE)
        for match in matches:
            try:
                # Clean up the path
                new_path = match.strip().strip(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C232: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, [])
            settings = config.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C233: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)} о {schedule.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C234: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    
    async def add_prompt_command(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C235: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {}).get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C236: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            
        except Exception as e:
            logger.error(f`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C237: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    
    async def prompts_history_command(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C238: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                    if timestamp_str:
                        dt = datetime.fromisoformat(timestamp_str.replace(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C239: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                    status = record.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C240: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    
    async def callback_handler(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C241: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            
            enabled = settings.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C242: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 30)
            retry_attempts = settings.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C243: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
            buttons.append(
                [InlineKeyboardButton(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C244: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
            buttons.append(
                [InlineKeyboardButton(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C245: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
            buttons.append([InlineKeyboardButton(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C246: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        for line in lines:
            if line.strip().startswith(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C247: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                
                for i, segment in enumerate(segments):
                    if i % 2 == 0:  # Outside inline code
                        # Escape only truly problematic characters for Telegram
                        segment = (segment
                                  .replace(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C248: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        for line in lines:
            line_length = len(line) + 1  # +1 for newline

            # Check for code block markers
            if line.strip() == `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C249: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        event = AuditEvent(
            timestamp=datetime.utcnow(),
            user_id=user_id,
            event_type=`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C250: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        if not token:
            logger.warning(
                `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C251: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        value = translation_dict
        
        for k in keys:
            if isinstance(value, dict) and k in value:
                value = value[k]
            else:
                # If key not found, track it and return the key itself as fallback
                self._track_missing_key(key, language)
                logger.warning(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C252: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            languages[lang_code] = lang_info.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C253: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `] for data in self.missing_keys.values())),
                `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C254: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage: Optional[UserLanguageStorage] = context.bot_data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C255: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            for block in message.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C256: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                tool_input = block.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C257: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                            or tool_input.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C258: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                            or tool_input.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C259: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
                        }
                    )

        logger.debug(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C260: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            for block in message.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C261: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                tool_input = block.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C262: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
                        }
                    )

        logger.debug(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C263: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            for block in message.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C264: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
                tool_results.append(
                    {
                        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C265: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
                    }
                )

        logger.debug(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C266: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) or msg.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C267: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
                    }
                )

            # Check for tool result errors
            if msg.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C268: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
                if result.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C269: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
                        }
                    )

        logger.debug(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C270: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
                for block in message.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C271: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        for line in lines:
            # Check for code block markers
            if line.strip().startswith(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C272: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) or tool_input.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C273: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if env_path and os.path.exists(env_path) and os.access(env_path, os.X_OK):
        return env_path

    # Check if claude is already in PATH
    claude_path = shutil.which(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C274: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        if claude_dir not in current_path:
            os.environ[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C275: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                if tool_name and tool_name not in self.tools_used:
                    self.tools_used.append(tool_name)

    def to_dict(self) -> Dict:
        `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C276: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `]),
            last_used=datetime.fromisoformat(data[`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C277: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 0.0),
            total_turns=data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C278: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 0),
            tools_used=data.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C279: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {}),
                        working_directory,
                        user_id,
                    )

                    if not valid:
                        tools_validated = False
                        validation_errors.append(error)

                        # Track blocked tools
                        if `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C280: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)[1]
                        blocked_tools.append(tool_name)

                # Create user-friendly error message
                if blocked_tools:
                    tool_list = `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C281: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) for call in self.tool_calls if call.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C282: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        elif self.is_error() and self.content:
            return self.content
        return None


class ClaudeProcessManager:
    `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C283: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 1)
                yield line.decode(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C284: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
        content_blocks = message.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C285: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `))
            elif block.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C286: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)},
            execution_id=msg.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C287: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
        content = result.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C288: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                    if isinstance(result, dict)
                    else None
                ),
            },
            timestamp=msg.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C289: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
        content = message.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C290: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `))
                elif isinstance(block, str):
                    text_parts.append(block)
            content = `

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C291: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, msg.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C292: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
            },
            timestamp=msg.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C293: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, msg.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C294: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
            },
            timestamp=msg.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C295: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
                for block in message.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C296: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
                            }
                        )

        return ClaudeResponse(
            content=result.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C297: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
            cost=result.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C298: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 0),
            num_turns=result.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C299: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, False),
            error_type=result.get(`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C300: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `session.session_cleared`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C301: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `messages.server_overloaded`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C302: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `messages.session_started`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C303: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `progress.processing_image`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C304: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `messages.command_executed`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C305: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `buttons.git_info`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C306: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `messages.welcome_back`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C307: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `messages.maintenance_mode`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C308: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `session.export_complete`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C309: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `commands.start.export_cmd`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C310: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `messages.authentication_success`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C311: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `progress.analyzing_image`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C312: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `buttons.continue_session`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C313: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `buttons.export_session`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C314: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `progress.review_file_default`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C315: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `messages.file_processed`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C316: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `messages.session_ended`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C317: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `progress.file_truncated_notice`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C318: ВІДСУТНІЙ УКРАЇНСЬКИЙ ПЕРЕКЛАД

**Ключ:** `session.export_session_progress`

**Що бачить користувач:** Замість тексту може показуватися ключ перекладу або англійський текст

**Рішення:** Додати переклад у `uk.json` файл


### C319: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📁 Show Projects`

**Callback:** `action:show_projects`

**Файл:** `src/bot/handlers/command.py` (рядок 117)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C320: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `❓ Get Help`

**Callback:** `action:help`

**Файл:** `src/bot/handlers/command.py` (рядок 118)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C321: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🆕 New Session`

**Callback:** `action:new_session`

**Файл:** `src/bot/handlers/command.py` (рядок 121)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C322: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Check Status`

**Callback:** `action:status`

**Файл:** `src/bot/handlers/command.py` (рядок 122)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C323: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `⬆️ Go Up`

**Callback:** `cd:..`

**Файл:** `src/bot/handlers/command.py` (рядок 576)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C324: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🏠 Go to Root`

**Callback:** `cd:/`

**Файл:** `src/bot/handlers/command.py` (рядок 577)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C325: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔄 Refresh`

**Callback:** `action:refresh_ls`

**Файл:** `src/bot/handlers/command.py` (рядок 583)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C326: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📁 List Files`

**Callback:** `action:ls`

**Файл:** `src/bot/handlers/command.py` (рядок 754)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C327: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📋 Projects`

**Callback:** `action:show_projects`

**Файл:** `src/bot/handlers/command.py` (рядок 755)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C328: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🏠 Go to Root`

**Callback:** `cd:/`

**Файл:** `src/bot/handlers/command.py` (рядок 815)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C329: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔄 Continue`

**Callback:** `action:continue`

**Файл:** `src/bot/handlers/command.py` (рядок 893)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C330: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📤 Export`

**Callback:** `action:export`

**Файл:** `src/bot/handlers/command.py` (рядок 910)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C331: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔄 Refresh`

**Callback:** `action:refresh_status`

**Файл:** `src/bot/handlers/command.py` (рядок 911)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C332: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📝 Markdown`

**Callback:** `export:markdown`

**Файл:** `src/bot/handlers/command.py` (рядок 964)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C333: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🌐 HTML`

**Callback:** `export:html`

**Файл:** `src/bot/handlers/command.py` (рядок 965)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C334: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📋 JSON`

**Callback:** `export:json`

**Файл:** `src/bot/handlers/command.py` (рядок 968)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C335: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `❌ Cancel`

**Callback:** `export:cancel`

**Файл:** `src/bot/handlers/command.py` (рядок 969)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C336: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🆕 New Session`

**Callback:** `action:new_session`

**Файл:** `src/bot/handlers/command.py` (рядок 1026)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C337: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Status`

**Callback:** `action:status`

**Файл:** `src/bot/handlers/command.py` (рядок 1032)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C338: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `❓ Help`

**Callback:** `action:help`

**Файл:** `src/bot/handlers/command.py` (рядок 1033)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C339: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Show Diff`

**Callback:** `git:diff`

**Файл:** `src/bot/handlers/command.py` (рядок 1225)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C340: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📜 Show Log`

**Callback:** `git:log`

**Файл:** `src/bot/handlers/command.py` (рядок 1226)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C341: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔄 Refresh`

**Callback:** `git:status`

**Файл:** `src/bot/handlers/command.py` (рядок 1229)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C342: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📁 Files`

**Callback:** `action:ls`

**Файл:** `src/bot/handlers/command.py` (рядок 1230)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C343: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `➕ Додати завдання`

**Callback:** `schedule:add`

**Файл:** `src/bot/handlers/command.py` (рядок 1284)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C344: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `⚙️ Налаштування`

**Callback:** `schedule:settings`

**Файл:** `src/bot/handlers/command.py` (рядок 1285)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C345: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `➕ Додати`

**Callback:** `schedule:add`

**Файл:** `src/bot/handlers/command.py` (рядок 1324)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C346: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📝 Редагувати`

**Callback:** `schedule:edit`

**Файл:** `src/bot/handlers/command.py` (рядок 1325)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C347: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `⚙️ Налаштування`

**Callback:** `schedule:settings`

**Файл:** `src/bot/handlers/command.py` (рядок 1328)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C348: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Статистика`

**Callback:** `schedule:stats`

**Файл:** `src/bot/handlers/command.py` (рядок 1329)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C349: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📝 Створити завдання`

**Callback:** `schedule:create_new`

**Файл:** `src/bot/handlers/command.py` (рядок 1355)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C350: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📋 Зі шаблону`

**Callback:** `schedule:from_template`

**Файл:** `src/bot/handlers/command.py` (рядок 1356)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C351: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔙 Назад`

**Callback:** `schedule:list`

**Файл:** `src/bot/handlers/command.py` (рядок 1357)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C352: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Status`

**Callback:** `action:status`

**Файл:** `src/bot/handlers/callback.py` (рядок 392)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C353: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🆕 New Session`

**Callback:** `action:new_session`

**Файл:** `src/bot/handlers/callback.py` (рядок 412)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C354: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Status`

**Callback:** `action:status`

**Файл:** `src/bot/handlers/callback.py` (рядок 418)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C355: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `❓ Help`

**Callback:** `action:help`

**Файл:** `src/bot/handlers/callback.py` (рядок 419)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C356: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔄 Continue`

**Callback:** `action:continue`

**Файл:** `src/bot/handlers/callback.py` (рядок 588)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C357: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔄 Refresh`

**Callback:** `action:refresh_status`

**Файл:** `src/bot/handlers/callback.py` (рядок 612)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C358: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📁 Projects`

**Callback:** `action:show_projects`

**Файл:** `src/bot/handlers/callback.py` (рядок 613)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C359: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `⬆️ Go Up`

**Callback:** `cd:..`

**Файл:** `src/bot/handlers/callback.py` (рядок 671)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C360: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🏠 Root`

**Callback:** `cd:/`

**Файл:** `src/bot/handlers/callback.py` (рядок 672)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C361: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔄 Refresh`

**Callback:** `action:refresh_ls`

**Файл:** `src/bot/handlers/callback.py` (рядок 678)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C362: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🧪 Run Tests`

**Callback:** `quick:test`

**Файл:** `src/bot/handlers/callback.py` (рядок 717)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C363: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📦 Install Deps`

**Callback:** `quick:install`

**Файл:** `src/bot/handlers/callback.py` (рядок 718)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C364: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🎨 Format Code`

**Callback:** `quick:format`

**Файл:** `src/bot/handlers/callback.py` (рядок 721)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C365: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔍 Find TODOs`

**Callback:** `quick:find_todos`

**Файл:** `src/bot/handlers/callback.py` (рядок 722)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C366: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔨 Build`

**Callback:** `quick:build`

**Файл:** `src/bot/handlers/callback.py` (рядок 725)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C367: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🚀 Start Server`

**Callback:** `quick:start`

**Файл:** `src/bot/handlers/callback.py` (рядок 726)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C368: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Git Status`

**Callback:** `quick:git_status`

**Файл:** `src/bot/handlers/callback.py` (рядок 729)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C369: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔧 Lint Code`

**Callback:** `quick:lint`

**Файл:** `src/bot/handlers/callback.py` (рядок 730)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C370: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `⬅️ Back`

**Callback:** `action:new_session`

**Файл:** `src/bot/handlers/callback.py` (рядок 732)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C371: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Status`

**Callback:** `action:status`

**Файл:** `src/bot/handlers/callback.py` (рядок 949)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C372: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `❓ Help`

**Callback:** `action:help`

**Файл:** `src/bot/handlers/callback.py` (рядок 950)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C373: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Show Diff`

**Callback:** `git:diff`

**Файл:** `src/bot/handlers/callback.py` (рядок 1014)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C374: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📜 Show Log`

**Callback:** `git:log`

**Файл:** `src/bot/handlers/callback.py` (рядок 1015)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C375: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔄 Refresh`

**Callback:** `git:status`

**Файл:** `src/bot/handlers/callback.py` (рядок 1018)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C376: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📁 Files`

**Callback:** `action:ls`

**Файл:** `src/bot/handlers/callback.py` (рядок 1019)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C377: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📜 Show Log`

**Callback:** `git:log`

**Файл:** `src/bot/handlers/callback.py` (рядок 1050)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C378: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Status`

**Callback:** `git:status`

**Файл:** `src/bot/handlers/callback.py` (рядок 1051)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C379: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Show Diff`

**Callback:** `git:diff`

**Файл:** `src/bot/handlers/callback.py` (рядок 1077)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C380: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Status`

**Callback:** `git:status`

**Файл:** `src/bot/handlers/callback.py` (рядок 1078)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C381: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📝 Створити завдання`

**Callback:** `schedule:create_new`

**Файл:** `src/bot/handlers/callback.py` (рядок 1265)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C382: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📋 Зі шаблону`

**Callback:** `schedule:from_template`

**Файл:** `src/bot/handlers/callback.py` (рядок 1266)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C383: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔙 Назад`

**Callback:** `schedule:list`

**Файл:** `src/bot/handlers/callback.py` (рядок 1267)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C384: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `➕ Додати завдання`

**Callback:** `schedule:add`

**Файл:** `src/bot/handlers/callback.py` (рядок 1287)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C385: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `⚙️ Налаштування`

**Callback:** `schedule:settings`

**Файл:** `src/bot/handlers/callback.py` (рядок 1288)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C386: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `➕ Додати`

**Callback:** `schedule:add`

**Файл:** `src/bot/handlers/callback.py` (рядок 1322)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C387: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📝 Редагувати`

**Callback:** `schedule:edit`

**Файл:** `src/bot/handlers/callback.py` (рядок 1323)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C388: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `⚙️ Налаштування`

**Callback:** `schedule:settings`

**Файл:** `src/bot/handlers/callback.py` (рядок 1326)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C389: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔄 Оновити`

**Callback:** `schedule:list`

**Файл:** `src/bot/handlers/callback.py` (рядок 1327)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C390: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🌙 Змінити DND`

**Callback:** `schedule:change_dnd`

**Файл:** `src/bot/handlers/callback.py` (рядок 1359)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C391: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `⚡ Налаштування`

**Callback:** `schedule:advanced`

**Файл:** `src/bot/handlers/callback.py` (рядок 1360)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C392: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔙 Назад`

**Callback:** `schedule:list`

**Файл:** `src/bot/handlers/callback.py` (рядок 1362)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C393: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📋 Детальні логи`

**Callback:** `schedule:logs`

**Файл:** `src/bot/handlers/callback.py` (рядок 1382)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C394: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔙 Назад`

**Callback:** `schedule:list`

**Файл:** `src/bot/handlers/callback.py` (рядок 1383)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C395: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔧 Налаштування`

**Callback:** `prompts_settings`

**Файл:** `src/bot/handlers/scheduled_prompts_handler.py` (рядок 54)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C396: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `💾 Save Code`

**Callback:** `save_code`

**Файл:** `src/bot/utils/formatting.py` (рядок 406)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C397: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📁 Show Files`

**Callback:** `show_files`

**Файл:** `src/bot/utils/formatting.py` (рядок 411)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C398: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔧 Debug`

**Callback:** `debug`

**Файл:** `src/bot/utils/formatting.py` (рядок 415)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C399: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔄 Continue`

**Callback:** `continue`

**Файл:** `src/bot/utils/formatting.py` (рядок 419)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C400: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `💡 Explain`

**Callback:** `explain`

**Файл:** `src/bot/utils/formatting.py` (рядок 420)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C401: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🧪 Test`

**Callback:** `quick:test`

**Файл:** `src/bot/utils/formatting.py` (рядок 579)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C402: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📦 Install`

**Callback:** `quick:install`

**Файл:** `src/bot/utils/formatting.py` (рядок 580)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C403: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🎨 Format`

**Callback:** `quick:format`

**Файл:** `src/bot/utils/formatting.py` (рядок 581)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C404: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔍 Find TODOs`

**Callback:** `quick:find_todos`

**Файл:** `src/bot/utils/formatting.py` (рядок 584)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C405: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `🔨 Build`

**Callback:** `quick:build`

**Файл:** `src/bot/utils/formatting.py` (рядок 585)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


### C406: НЕПРАЦЮЮЧА КНОПКА

**Текст кнопки:** `📊 Git Status`

**Callback:** `quick:git_status`

**Файл:** `src/bot/utils/formatting.py` (рядок 586)

**Що бачить користувач:** Натискає кнопку → нічого не відбувається або помилка

**Рішення:** Реалізувати обробник або прибрати кнопку


## 🟠 ПРОБЛЕМИ ВИСОКОГО ПРІОРИТЕТУ (ВИПРАВИТИ ЦЬОГО ТИЖНЯ)

### H1: ЗМІШАНІ МОВИ

**Файл:** `src/bot/handlers/command.py` (рядок 1241)

**Фрагмент:** `❌ **Git Error`

**Що бачить користувач:** Український інтерфейс з англійськими помилками

**Рішення:** Перекласти повідомлення повністю українською


### H2: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/command.py` (рядок 235)

**Текст:** `❌ Settings not available`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H3: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/command.py` (рядок 317)

**Текст:** `❌ Settings not available`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H4: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/command.py` (рядок 519)

**Текст:** `❌ Settings not available`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H5: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/command.py` (рядок 623)

**Текст:** `❌ Settings not available`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H6: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/command.py` (рядок 740)

**Текст:** `❌ Settings not available`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H7: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/command.py` (рядок 779)

**Текст:** `❌ Settings not available`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H8: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/command.py` (рядок 849)

**Текст:** `❌ Settings not available`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H9: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/command.py` (рядок 993)

**Текст:** `❌ Settings not available`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H10: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/command.py` (рядок 1066)

**Текст:** `❌ Settings not available`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H11: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/command.py` (рядок 1154)

**Текст:** `❌ Settings not available`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H12: ЗМІШАНІ МОВИ

**Файл:** `src/bot/handlers/callback.py` (рядок 1100)

**Фрагмент:** `❌ **Git Error`

**Що бачить користувач:** Український інтерфейс з англійськими помилками

**Рішення:** Перекласти повідомлення повністю українською


### H13: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/callback.py` (рядок 225)

**Текст:** `❓ **Unknown confirmation response**`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H14: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/callback.py` (рядок 1184)

**Текст:** `❌ Localization system not available`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H15: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/callback.py` (рядок 1257)

**Текст:** `❌ Помилка доступу до системи`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H16: ЗМІШАНІ МОВИ

**Файл:** `src/bot/handlers/message.py` (рядок 124)

**Фрагмент:** `❌ **Claude Code Error`

**Що бачить користувач:** Український інтерфейс з англійськими помилками

**Рішення:** Перекласти повідомлення повністю українською


### H17: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/scheduled_prompts_handler.py` (рядок 64)

**Текст:** `❌ Помилка при завантаженні списку завдань`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H18: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/scheduled_prompts_handler.py` (рядок 118)

**Текст:** `❌ Помилка при зміні стану системи`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H19: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/scheduled_prompts_handler.py` (рядок 126)

**Текст:** `📊 **Історія виконання порожня**`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H20: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/scheduled_prompts_handler.py` (рядок 137)

**Текст:** `📊 **Історія виконання порожня**`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H21: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/scheduled_prompts_handler.py` (рядок 173)

**Текст:** `❌ Помилка при завантаженні історії`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H22: ЖОРСТКО ЗАКОДОВАНИЙ РЯДОК

**Файл:** `src/bot/handlers/scheduled_prompts_handler.py` (рядок 219)

**Текст:** `❌ Помилка при завантаженні налаштувань`

**Що бачить користувач:** Текст, який не змінюється при зміні мови

**Рішення:** Винести текст у файли перекладів та використовувати функцію локалізації


### H23: ЗЛАМАНИЙ ШЛЯХ КОРИСТУВАЧА

**Шлях:** `start_new_session`

**Проблеми:** Команда /start не реалізована, Команда /new не реалізована, Команда /ls не реалізована, Команда /cd не реалізована, Команда /help не реалізована

**Що бачить користувач:** Не може завершити очікувану дію

**Рішення:** Реалізувати відсутні команди або обробники кнопок


### H24: ЗЛАМАНИЙ ШЛЯХ КОРИСТУВАЧА

**Шлях:** `quick_actions`

**Проблеми:** Команда /actions не реалізована, Callback continue не має обробника, Callback export_session не має обробника, Callback save_code не має обробника

**Що бачить користувач:** Не може завершити очікувану дію

**Рішення:** Реалізувати відсутні команди або обробники кнопок


### H25: ЗЛАМАНИЙ ШЛЯХ КОРИСТУВАЧА

**Шлях:** `project_management`

**Проблеми:** Команда /projects не реалізована, Команда /git не реалізована, Команда /schedules не реалізована

**Що бачить користувач:** Не може завершити очікувану дію

**Рішення:** Реалізувати відсутні команди або обробники кнопок


### H26: ЗЛАМАНИЙ ШЛЯХ КОРИСТУВАЧА

**Шлях:** `settings`

**Проблеми:** Команда /settings не реалізована, Callback lang:select не має обробника, Callback toggle_language не має обробника

**Що бачить користувач:** Не може завершити очікувану дію

**Рішення:** Реалізувати відсутні команди або обробники кнопок


### H27: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/main.py` (рядок 16)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H28: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/main.py` (рядок 156)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H29: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/main.py` (рядок 203)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H30: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/main.py` (рядок 17)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H31: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/main.py` (рядок 152)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H32: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/bot/handlers/command.py` (рядок 8)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H33: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/bot/handlers/command.py` (рядок 381)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H34: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/bot/handlers/command.py` (рядок 411)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H35: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/bot/handlers/callback.py` (рядок 7)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H36: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/bot/handlers/callback.py` (рядок 444)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H37: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/bot/handlers/callback.py` (рядок 790)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H38: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/claude/facade.py` (рядок 21)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H39: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/claude/facade.py` (рядок 13)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H40: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/claude/facade.py` (рядок 27)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H41: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/claude/facade.py` (рядок 39)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H42: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/claude/__init__.py` (рядок 10)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H43: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/claude/__init__.py` (рядок 29)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H44: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/claude/__init__.py` (рядок 11)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H45: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/claude/__init__.py` (рядок 31)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H46: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `claude_cli`

**Файл:** `src/claude/integration.py` (рядок 91)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H47: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/bot/features/file_handler.py` (рядок 224)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H48: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/bot/features/file_handler.py` (рядок 264)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H49: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/bot/features/file_handler.py` (рядок 221)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H50: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/bot/features/file_handler.py` (рядок 222)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H51: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/bot/features/file_handler.py` (рядок 227)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H52: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/bot/features/scheduled_prompts.py` (рядок 381)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H53: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/bot/features/scheduled_prompts.py` (рядок 381)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H54: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/bot/features/availability_monitor.py` (рядок 245)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H55: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/bot/features/availability_monitor.py` (рядок 269)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H56: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/bot/features/availability_monitor.py` (рядок 406)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H57: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/bot/handlers/scheduled_prompts_handler.py` (рядок 130)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H58: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/bot/handlers/scheduled_prompts_handler.py` (рядок 130)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H59: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/localization/manager.py` (рядок 165)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H60: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/localization/manager.py` (рядок 165)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H61: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/claude/sdk_integration.py` (рядок 53)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H62: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/claude/sdk_integration.py` (рядок 58)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H63: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/claude/sdk_integration.py` (рядок 69)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H64: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/claude/sdk_integration.py` (рядок 71)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H65: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/claude/sdk_integration.py` (рядок 72)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H66: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/claude/sdk_integration.py` (рядок 77)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H67: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/claude/sdk_integration.py` (рядок 95)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H68: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `file_system`

**Файл:** `src/claude/sdk_integration.py` (рядок 62)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H69: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `database`

**Файл:** `src/main.py` (рядок 18)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H70: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `database`

**Файл:** `src/main.py` (рядок 142)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H71: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `database`

**Файл:** `src/claude/session.py` (рядок 154)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H72: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `database`

**Файл:** `src/claude/facade.py` (рядок 16)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H73: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `database`

**Файл:** `src/claude/facade.py` (рядок 29)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H74: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `database`

**Файл:** `src/claude/__init__.py` (рядок 17)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H75: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `database`

**Файл:** `src/claude/__init__.py` (рядок 34)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H76: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `docker`

**Файл:** `src/bot/features/session_export.py` (рядок 201)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H77: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `docker`

**Файл:** `src/bot/features/session_export.py` (рядок 256)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H78: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `docker`

**Файл:** `src/bot/features/availability_monitor.py` (рядок 161)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


### H79: ІНТЕГРАЦІЯ БЕЗ ОБРОБКИ ПОМИЛОК

**Тип інтеграції:** `docker`

**Файл:** `src/bot/features/availability_monitor.py` (рядок 163)

**Що бачить користувач:** Технічні помилки замість зрозумілих повідомлень

**Рішення:** Додати try-except блоки з локалізованими повідомленнями про помилки


## 🟡 ПРОБЛЕМИ СЕРЕДНЬОГО ПРІОРИТЕТУ (ПОЛІПШЕННЯ ІНТЕРФЕЙСУ)

### M1: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
    )

    parser.add_argument(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M2: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    return parser.parse_args()


async def create_application(config: Settings) -> Dict[str, Any]:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M3: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        sys.exit(0)


if __name__ == `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M4: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) if chat_id.strip()]
        if isinstance(v, int):
            return [v]
        if isinstance(v, list):
            return v
        return []


class Settings(BaseSettings):
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M5: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) if uid.strip()]
    #     if isinstance(v, int):
    #         return [v]  # Convert single int to list
    #     if isinstance(v, list):
    #         return v  # Already a list
    #     # If we can`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M6: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `])

        # Parse JSON fields
        if data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M7: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `])

        # Parse JSON fields
        if data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M8: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {}),
                    timestamp=datetime.utcnow(),
                    success=not response.is_error,
                    error_message=response.error_type if response.is_error else None,
                )
                await self.tools.save_tool_usage(tool_usage)

        # Update cost tracking
        await self.costs.update_daily_cost(user_id, response.cost)

        # Update user stats
        user = await self.users.get_user(user_id)
        if user:
            user.total_cost += response.cost
            user.message_count += 1
            user.last_active = datetime.utcnow()
            await self.users.update_user(user)

        # Update session stats
        session = await self.sessions.get_session(session_id)
        if session:
            session.total_cost += response.cost
            session.total_turns += response.num_turns
            session.message_count += 1
            session.last_used = datetime.utcnow()
            await self.sessions.update_session(session)

        # Log audit event
        audit_event = AuditLogModel(
            id=None,
            user_id=user_id,
            event_type=`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M9: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
            security=self.deps.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M10: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )

        # Try to notify user
        if update and update.effective_message:
            try:
                await update.effective_message.reply_text(user_message)
            except Exception:
                logger.exception(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M11: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        if audit_logger and update and update.effective_user:
            try:
                await audit_logger.log_security_violation(
                    user_id=update.effective_user.id,
                    violation_type=`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M12: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger = data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M13: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return  # Block processing

    # Validate file uploads if present
    if message and message.document:
        is_safe, error_message = await validate_file_upload(
            message.document, security_validator, user_id, audit_logger
        )
        if not is_safe:
            await message.reply_text(
                f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M14: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    # Track user behavior patterns
    user_behavior = data.setdefault(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M15: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `: None,
        },
    )

    import time

    current_time = time.time()

    if user_data[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M16: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `This activity has been logged.\n\n`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M17: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger = data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M18: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
        return

    # Check if user is already authenticated
    if auth_manager.is_authenticated(user_id):
        # Update session activity
        if auth_manager.refresh_session(user_id):
            session = auth_manager.get_session(user_id)
            logger.debug(
                `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M19: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        )

    if authentication_successful:
        session = auth_manager.get_session(user_id)
        logger.info(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M20: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `Please contact the administrator for access.\n\n`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M21: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not auth_manager or not auth_manager.is_authenticated(user_id):
        if event.effective_message:
            await event.effective_message.reply_text(
                `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M22: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not auth_manager or not auth_manager.is_authenticated(user_id):
        if event.effective_message:
            await event.effective_message.reply_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M23: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
        return

    # Check for admin permissions (placeholder logic)
    permissions = session.user_info.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M24: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger = data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M25: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        return  # Stop processing

    # Rate limit check passed
    logger.debug(
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M26: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    # Store start time for duration tracking
    import time

    start_time = time.time()

    try:
        # Execute the handler
        result = await handler(event, data)

        # Calculate processing time
        processing_time = time.time() - start_time

        # Get actual cost from context if available
        actual_cost = data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M27: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
    user_burst_data = burst_tracker.setdefault(
        user_id, {`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M28: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `re sending requests too quickly. `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M29: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
                )
        else:
            if event.effective_message:
                await event.effective_message.reply_text(
                    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M30: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) for tool in response.tools_used]

        # Update active files if file tools were used
        if any(tool in self.last_tools_used for tool in [`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M31: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) for tool in response.tools_used]

        if `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M32: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
            lines.append(f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M33: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `).isoformat()
                    if session.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M34: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, [])
            for msg in recent_messages:
                content = msg.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M35: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `quick_actions.{action.id}.name`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M36: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        # Detect language
        language = self._detect_language(file_path.suffix)

        # Create prompt
        prompt = f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M37: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `).lower()
                for framework in possible_frameworks:
                    if framework.lower() in content:
                        frameworks.append(framework)

        # Check for specific framework files
        if (directory / `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M38: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                    # Count TODOs and FIXMEs
                    todo_count += content.upper().count(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M39: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
        
        # Check Claude availability
        if conditions.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M40: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, False):
            if not self._is_dnd_time():
                return False, `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M41: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 0)
        if no_activity_hours > 0:
            if await self._check_user_activity(no_activity_hours):
                return False, f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M42: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
        if not schedule:
            return False
        
        timezone = ZoneInfo(schedule.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M43: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        target_time_str = schedule.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M44: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `).lower()
            day_map = {
                `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M45: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        logger.info(f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M46: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            success, output = await self._execute_claude_prompt(prompt_text)
            
            if success:
                logger.info(f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M47: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {}).get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M48: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {}).get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M49: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, [])
        if not prompts:
            return
        
        # Check if any prompts need execution
        prompts_to_execute = []
        for prompt in prompts:
            if self._is_time_to_execute(prompt):
                prompts_to_execute.append(prompt)
        
        if not prompts_to_execute:
            return
        
        logger.info(f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M50: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `))
                    last_execution = dt.strftime(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M51: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            if localization:
                result = localization.get(key, language=`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M52: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `: None}))
        if not self.transitions_log.exists():
            self.transitions_log.touch()

    def parse_limit_message(self, output: str) -> Optional[datetime]:
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M53: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 
                                         timestamp=now.strftime(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M54: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 
                                         timestamp=now.strftime(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M55: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 
                                                time=reset_local.strftime(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M56: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, False)
            last_reason = last_state_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M57: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            last_reset_expected = datetime.fromisoformat(last_reset_expected_str) if last_reset_expected_str else None
            last_check_str = last_state_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M58: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
            if line.startswith(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M59: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)) == 4:
                # Commit info line
                parts = line.split(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M60: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M61: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, name=update.effective_user.first_name)
        description_text = await t(context, user_id, `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M62: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if audit_logger:
        audit_logger_typed = cast(AuditLogger, audit_logger)
        await audit_logger_typed.log_command(
            user_id=user_id, command=`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M63: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M64: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {}).get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M65: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M66: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings_typed.approved_directory
    ) if context.user_data else settings_typed.approved_directory
    relative_path = current_dir.relative_to(settings_typed.approved_directory)

    # Clear any existing session data
    if context.user_data:
        context.user_data[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M67: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M68: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M69: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M70: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings_typed.approved_directory
    ) if context.user_data else settings_typed.approved_directory

    status_msg = None
    try:
        if not claude_integration:
            # Get localized error message
            localization = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M71: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) if context.user_data else None

        if claude_session_id:
            # We have a session in context, continue it directly
            # Get localized continuation messages
            localization = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M72: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, relative_path=str(current_dir.relative_to(settings_typed.approved_directory)))
                
                if prompt:
                    process_msg = await t(context, user_id, `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M73: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            user_language_storage = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M74: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `Directory: `{current_dir.relative_to(settings_typed.approved_directory)}/`\n\n`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M75: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
        )

        # Log failed continue
        if audit_logger:
            audit_logger_typed = cast(AuditLogger, audit_logger)
            await audit_logger_typed.log_command(
                user_id=user_id,
                command=`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M76: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M77: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    # Get current directory
    current_dir = context.user_data.get(
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M78: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M79: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M80: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `• `/cd ..` - Go up one level\n`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M81: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings_typed.approved_directory
    ) if context.user_data else settings_typed.approved_directory

    try:
        # Validate path using security validator
        if security_validator:
            security_validator_typed = cast(SecurityValidator, security_validator)
            valid, resolved_path, error = security_validator_typed.validate_path(
                target_path, current_dir
            )

            if not valid:
                await message.reply_text(f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M82: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return

        if not resolved_path.is_dir():
            await message.reply_text(
                f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M83: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
        )

        # Log successful command
        if audit_logger:
            audit_logger_typed = cast(AuditLogger, audit_logger)
            await audit_logger_typed.log_command(user_id, `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M84: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M85: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings_typed.approved_directory
    ) if context.user_data else settings_typed.approved_directory

    relative_path = current_dir.relative_to(settings_typed.approved_directory)
    absolute_path = str(current_dir)

    # Add quick navigation buttons
    keyboard = [
        [
            InlineKeyboardButton(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M86: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        parse_mode=None,
        reply_markup=reply_markup,
    )


async def show_projects(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M87: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M88: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return

        # Create inline keyboard with project buttons
        keyboard = []
        for i in range(0, len(projects), 2):
            row = []
            for j in range(2):
                if i + j < len(projects):
                    project = projects[i + j]
                    row.append(
                        InlineKeyboardButton(
                            f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M89: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
            reply_markup=reply_markup,
        )

    except Exception as e:
        await message.reply_text(f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M90: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M91: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) if context.user_data else None
    current_dir = context.user_data.get(
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M92: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            current_cost = cost_usage.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M93: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings_typed.claude_max_cost_per_user)
            cost_percentage = (current_cost / cost_limit) * 100 if cost_limit > 0 else 0

            usage_info = f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M94: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `.join(status_lines), parse_mode=None, reply_markup=reply_markup
    )


async def export_session(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M95: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    # Check if session export is available
    session_exporter = features.get_session_export() if features else None

    if not session_exporter:
        await message.reply_text(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M96: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) if context.user_data else None

    if not claude_session_id:
        await message.reply_text(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M97: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        parse_mode=None,
        reply_markup=reply_markup,
    )


async def end_session(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M98: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M99: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) if context.user_data else None

    if not claude_session_id:
        await message.reply_text(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M100: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings_typed.approved_directory
    ) if context.user_data else settings_typed.approved_directory
    relative_path = current_dir.relative_to(settings_typed.approved_directory)

    # Clear session data
    if context.user_data:
        context.user_data[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M101: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        parse_mode=None,
        reply_markup=reply_markup,
    )

    logger.info(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M102: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M103: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not features or not features.is_enabled(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M104: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )
        return

    # Get current directory
    current_dir = context.user_data.get(
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M105: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return

        # Get context-aware actions
        actions = await quick_action_manager.get_suggestions(
            session_data={`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M106: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return

        # Create inline keyboard with localization
        # user_id already defined above
        localization = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M107: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        user_lang = None
        
        if user_language_storage:
            try:
                user_lang = await user_language_storage.get_user_language(user_id)
            except:
                pass
        
        keyboard = quick_action_manager.create_inline_keyboard(
            actions, columns=2, localization=localization, user_lang=user_lang
        )

        # Get localized title for quick actions
        title_text = await t(context, user_id, `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M108: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        await message.reply_text(error_text, parse_mode=None)
        logger.error(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M109: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not settings:
        await message.reply_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M110: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not features or not features.is_enabled(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M111: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )
        return

    # Get current directory
    current_dir = context.user_data.get(
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M112: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `Current directory `{current_dir.relative_to(settings_typed.approved_directory)}/` is not a git repository.\n\n`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M113: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        
        if not application or not settings:
            await message.reply_text(
                `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M114: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, [])
        system_settings = config.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M115: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `під час DND періоду (23:00-08:00).\n\n`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M116: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)} о {schedule.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M117: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M118: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )

    except Exception as e:
        logger.error(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M119: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `An error occurred while processing your request.\n`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M120: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )


async def handle_cd_callback(
    query, project_name: str, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M121: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger: AuditLogger = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M122: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
        )

        # Handle special paths
        if project_name == `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M123: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                return
            # Use the validated path
            new_path = resolved_path

        # Check if directory exists
        if not new_path.exists() or not new_path.is_dir():
            await query.edit_message_text(
                f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M124: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
            reply_markup=reply_markup,
        )

        # Log successful directory change
        if audit_logger:
            await audit_logger.log_command(
                user_id=user_id, command=`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M125: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )


async def handle_confirm_callback(
    query, confirmation_type: str, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M126: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    else:
        await query.edit_message_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M127: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return

        # Create project buttons
        keyboard = []
        for i in range(0, len(projects), 2):
            row = []
            for j in range(2):
                if i + j < len(projects):
                    project = projects[i + j]
                    row.append(
                        InlineKeyboardButton(
                            f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M128: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        
        keyboard.append(
            [
                InlineKeyboardButton(root_text, callback_data=`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M129: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
            reply_markup=reply_markup,
        )

    except Exception as e:
        await query.edit_message_text(f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M130: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
    )
    relative_path = current_dir.relative_to(settings.approved_directory)

    # Get localized button text
    user_id = query.from_user.id
    start_coding_text = await get_localized_text(context, user_id, `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M131: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        parse_mode=None,
        reply_markup=reply_markup,
    )


async def _handle_end_session_action(query, context: ContextTypes.DEFAULT_TYPE) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M132: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not claude_session_id:
        await query.edit_message_text(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M133: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
    )
    relative_path = current_dir.relative_to(settings.approved_directory)

    # Clear session data
    context.user_data[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M134: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        parse_mode=None,
        reply_markup=reply_markup,
    )


async def _handle_continue_action(query, context: ContextTypes.DEFAULT_TYPE) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M135: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    current_dir = context.user_data.get(
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M136: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        if claude_session_id:
            # Continue with the existing session (no prompt = use --continue)
            await query.edit_message_text(
                f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M137: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
                parse_mode=None,
            )

            claude_response = await claude_integration.continue_session(
                user_id=user_id,
                working_directory=current_dir,
                prompt=None,  # No prompt = use --continue
            )

        if claude_response:
            # Update session ID in context
            context.user_data[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M138: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** ` if len(claude_response.content) > 500 else `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M139: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `Directory: `{current_dir.relative_to(settings.approved_directory)}/`\n\n`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M140: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    current_dir = context.user_data.get(
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M141: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            current_cost = cost_usage.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M142: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.claude_max_cost_per_user)
            cost_percentage = (current_cost / cost_limit) * 100 if cost_limit > 0 else 0

            usage_info = f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M143: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `.join(status_lines), parse_mode=None, reply_markup=reply_markup
    )


async def _handle_ls_action(query, context: ContextTypes.DEFAULT_TYPE) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M144: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
    )

    try:
        # List directory contents (similar to /ls command)
        items = []
        directories = []
        files = []

        for item in sorted(current_dir.iterdir()):
            if item.name.startswith(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M145: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
    )


async def _handle_quick_actions_action(
    query, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M146: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
        parse_mode=None,
        reply_markup=reply_markup,
    )


async def _handle_refresh_status_action(
    query, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M147: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
    )


async def handle_quick_action_callback(
    query, action_id: str, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M148: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M149: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        await query.edit_message_text(error_text, parse_mode=None)
        return

    # Get Claude integration
    claude_integration: ClaudeIntegration = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M150: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        await query.edit_message_text(error_text, parse_mode=None)
        return

    settings: Settings = context.bot_data[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M151: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
    )

    try:
        # Get the action from the manager
        action = quick_actions.actions.get(action_id)
        if not action:
            error_text = await get_localized_text(context, user_id, `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M152: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `quick_actions.{action.id}.name`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M153: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, action=action_display_name)
            await query.edit_message_text(error_text, parse_mode=None)
            return

        # Show execution message
        executing_text = await get_localized_text(context, user_id, `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M154: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, action=action_display_name)
            response_text = claude_response.content
            if len(response_text) > 4000:
                response_text = response_text[:4000] + `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M155: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, action=action_display_name)
            await query.edit_message_text(failed_text, parse_mode=None)

    except Exception as e:
        logger.error(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M156: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, action=action_id, error=str(e))
        await query.edit_message_text(error_text, parse_mode=None)


async def handle_followup_callback(
    query, suggestion_hash: str, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M157: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not conversation_enhancer:
        await query.edit_message_text(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M158: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )

        logger.info(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M159: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )


async def handle_conversation_callback(
    query, action_type: str, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M160: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `_Just type your request or upload files._`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M161: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        if conversation_enhancer:
            conversation_enhancer.clear_context(user_id)

        # Clear session data
        context.user_data[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M162: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
        )
        relative_path = current_dir.relative_to(settings.approved_directory)

        # Create quick action buttons
        keyboard = [
            [
                InlineKeyboardButton(
                    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M163: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
            reply_markup=reply_markup,
        )

        logger.info(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M164: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )


async def handle_git_callback(
    query, git_action: str, context: ContextTypes.DEFAULT_TYPE
) -> None:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M165: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    if not features or not features.is_enabled(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M166: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )
        return

    current_dir = context.user_data.get(
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M167: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )

    except Exception as e:
        logger.error(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M168: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
        )
        return

    session_exporter = features.get_session_export() if features else None
    if not session_exporter:
        await query.edit_message_text(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M169: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if not claude_session_id:
        await query.edit_message_text(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M170: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `Generating {export_format.upper()} export...`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M171: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `Format: {exported_session.format.upper()}\n`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M172: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            parse_mode=None,
        )

    except Exception as e:
        logger.error(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M173: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M174: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        keyboard.append([InlineKeyboardButton(back_text, callback_data=`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M175: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        
        await query.edit_message_text(select_message, reply_markup=reply_markup)
        
    elif param.startswith(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M176: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 1)[1]
        
        if localization.is_language_available(new_language):
            success = await user_language_storage.set_user_language(user_id, new_language)
            
            if success:
                # Get language name for confirmation
                lang_name = localization.get_available_languages().get(new_language, new_language.upper())
                
                # Get confirmation message in NEW language
                confirmation_text = localization.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M177: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                await query.edit_message_text(error_text)
        else:
            error_text = await get_user_text(localization, user_language_storage, user_id, `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M178: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        
        if not application or not settings:
            await query.edit_message_text(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M179: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, [])
            system_settings = config.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M180: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
                    reply_markup=reply_markup
                )
                return
            
            enabled_count = sum(1 for p in prompts if p.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M181: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)} о {schedule.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M182: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            
            enabled = system_settings.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M183: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            dnd_end = system_settings.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M184: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 0):.1f}с\n`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M185: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            
    except Exception as e:
        logger.error(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M186: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
            # Try to extract tool name from context if available
            tool_name = update_obj.metadata.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M187: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
                time_ms = update_obj.metadata[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M188: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            total_steps = update_obj.progress.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M189: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, []))
            model = update_obj.metadata.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M190: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    audit_logger: Optional[AuditLogger] = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M191: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                return

        # Send typing indicator
        await update.message.chat.send_action(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M192: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
            reply_to_message_id=update.message.message_id,
        )

        # Get Claude integration and storage from context
        claude_integration = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M193: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        if not claude_integration:
            await update.message.reply_text(
                `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M194: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
        )

        # Get existing session ID
        session_id = context.user_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M195: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
            except Exception as e:
                logger.warning(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M196: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
                    reply_to_message_id=update.message.message_id if i == 0 else None,
                )

        # Update session info
        context.user_data[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M197: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        conversation_enhancer = (
            features.get_conversation_enhancer() if features else None
        )

        if conversation_enhancer and claude_response:
            try:
                # Update conversation context
                conversation_enhancer.update_context(user_id, claude_response)

                # Check if we should show follow-up suggestions
                if conversation_enhancer.should_show_suggestions(claude_response):
                    # Generate follow-up suggestions
                    suggestions = conversation_enhancer.generate_follow_up_suggestions(
                        claude_response.content,
                        claude_response.tools_used or [],
                        conversation_context,
                    )

                    if suggestions:
                        # Create keyboard with suggestions
                        suggestion_keyboard = (
                            conversation_enhancer.create_follow_up_keyboard(suggestions)
                        )

                        # Send follow-up suggestions
                        await update.message.reply_text(
                            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M198: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
    )
    audit_logger: Optional[AuditLogger] = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M199: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

    logger.info(
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M200: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
                )

                # Log security violation
                if audit_logger:
                    await audit_logger.log_security_violation(
                        user_id=user_id,
                        violation_type=`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M201: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            return

        # Check rate limit for file processing
        file_cost = _estimate_file_processing_cost(document.file_size)
        if rate_limiter:
            allowed, limit_message = await rate_limiter.check_rate_limit(
                user_id, file_cost
            )
            if not allowed:
                await update.message.reply_text(f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M202: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, parse_mode=None
        )

        # Check if enhanced file handler is available
        features = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M203: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
                    parse_mode=None,
                )

            except Exception as e:
                logger.warning(
                    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M204: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `• Source code files (.py, .js, .ts, etc.)\n`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M205: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, parse_mode=None
        )

        # Get Claude integration from context
        claude_integration = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M206: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
                parse_mode=None,
            )
            return

        # Get current directory and session
        current_dir = context.user_data.get(
            `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M207: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        # Process with Claude
        try:
            claude_response = await claude_integration.run_command(
                prompt=prompt,
                working_directory=current_dir,
                user_id=user_id,
                session_id=session_id,
            )

            # Update session ID
            context.user_data[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M208: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    image_handler = features.get_image_handler() if features else None

    if image_handler:
        try:
            # Send processing indicator
            progress_msg = await update.message.reply_text(
                `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M209: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, parse_mode=None
            )

            # Get Claude integration
            claude_integration = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M210: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `,
                    parse_mode=None,
                )
                return

            # Get current directory and session
            current_dir = context.user_data.get(
                `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M211: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

            # Process with Claude
            try:
                claude_response = await claude_integration.run_command(
                    prompt=processed_image.prompt,
                    working_directory=current_dir,
                    user_id=user_id,
                    session_id=session_id,
                )

                # Update session ID
                context.user_data[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M212: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `• Text files (.py, .js, .md, etc.)\n`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M213: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, settings.approved_directory
    )

    for pattern in patterns:
        matches = re.findall(pattern, content, re.MULTILINE | re.IGNORECASE)
        for match in matches:
            try:
                # Clean up the path
                new_path = match.strip().strip(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M214: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, [])
            settings = config.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M215: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)} о {schedule.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M216: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    
    async def add_prompt_command(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M217: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {}).get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M218: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `
            )
            
        except Exception as e:
            logger.error(f`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M219: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    
    async def prompts_history_command(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M220: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                    if timestamp_str:
                        dt = datetime.fromisoformat(timestamp_str.replace(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M221: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                    status = record.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M222: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    
    async def callback_handler(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M223: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            
            enabled = settings.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M224: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 30)
            retry_attempts = settings.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M225: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
            buttons.append(
                [InlineKeyboardButton(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M226: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
            buttons.append(
                [InlineKeyboardButton(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M227: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `):
            buttons.append([InlineKeyboardButton(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M228: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        for line in lines:
            if line.strip().startswith(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M229: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                
                for i, segment in enumerate(segments):
                    if i % 2 == 0:  # Outside inline code
                        # Escape only truly problematic characters for Telegram
                        segment = (segment
                                  .replace(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M230: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        for line in lines:
            line_length = len(line) + 1  # +1 for newline

            # Check for code block markers
            if line.strip() == `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M231: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        event = AuditEvent(
            timestamp=datetime.utcnow(),
            user_id=user_id,
            event_type=`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M232: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        if not token:
            logger.warning(
                `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M233: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        value = translation_dict
        
        for k in keys:
            if isinstance(value, dict) and k in value:
                value = value[k]
            else:
                # If key not found, track it and return the key itself as fallback
                self._track_missing_key(key, language)
                logger.warning(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M234: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            languages[lang_code] = lang_info.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M235: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `] for data in self.missing_keys.values())),
                `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M236: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    user_language_storage: Optional[UserLanguageStorage] = context.bot_data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M237: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            for block in message.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M238: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                tool_input = block.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M239: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                            or tool_input.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M240: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                            or tool_input.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M241: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
                        }
                    )

        logger.debug(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M242: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            for block in message.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M243: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                tool_input = block.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M244: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
                        }
                    )

        logger.debug(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M245: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
            for block in message.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M246: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
                tool_results.append(
                    {
                        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M247: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
                    }
                )

        logger.debug(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M248: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) or msg.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M249: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
                    }
                )

            # Check for tool result errors
            if msg.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M250: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
                if result.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M251: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
                        }
                    )

        logger.debug(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M252: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
                for block in message.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M253: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        for line in lines:
            # Check for code block markers
            if line.strip().startswith(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M254: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) or tool_input.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M255: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
    if env_path and os.path.exists(env_path) and os.access(env_path, os.X_OK):
        return env_path

    # Check if claude is already in PATH
    claude_path = shutil.which(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M256: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)

        if claude_dir not in current_path:
            os.environ[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M257: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                if tool_name and tool_name not in self.tools_used:
                    self.tools_used.append(tool_name)

    def to_dict(self) -> Dict:
        `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M258: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `]),
            last_used=datetime.fromisoformat(data[`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M259: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 0.0),
            total_turns=data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M260: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 0),
            tools_used=data.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M261: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {}),
                        working_directory,
                        user_id,
                    )

                    if not valid:
                        tools_validated = False
                        validation_errors.append(error)

                        # Track blocked tools
                        if `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M262: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)[1]
                        blocked_tools.append(tool_name)

                # Create user-friendly error message
                if blocked_tools:
                    tool_list = `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M263: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `) for call in self.tool_calls if call.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M264: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
        elif self.is_error() and self.content:
            return self.content
        return None


class ClaudeProcessManager:
    `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M265: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 1)
                yield line.decode(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M266: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
        content_blocks = message.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M267: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `))
            elif block.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M268: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)},
            execution_id=msg.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M269: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
        content = result.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M270: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `)
                    if isinstance(result, dict)
                    else None
                ),
            },
            timestamp=msg.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M271: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
        content = message.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M272: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `))
                elif isinstance(block, str):
                    text_parts.append(block)
            content = `

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M273: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, msg.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M274: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
            },
            timestamp=msg.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M275: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, msg.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M276: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
            },
            timestamp=msg.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M277: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, {})
                for block in message.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M278: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
                            }
                        )

        return ClaudeResponse(
            content=result.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M279: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `),
            cost=result.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M280: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, 0),
            num_turns=result.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


### M281: ВІДСУТНІЙ АНГЛІЙСЬКИЙ ПЕРЕКЛАД

**Ключ:** `, False),
            error_type=result.get(`

**Рішення:** Додати переклад у `en.json` файл для підтримки англомовних користувачів


## 📈 МЕТРИКИ ЯКОСТІ

- **Покриття українською локалізацією:** 95.2%

- **Критичних проблем:** 406

- **Жорстко закодованих рядків:** 19

- **Відсутніх українських перекладів:** 300

- **Реалізованих оголошених команд:** 0 з 18
